<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:allowBackup="true"
        android:label="WYU-Monet"
        android:description="@string/app_description"
        android:supportsRtl="true">
        <activity
            android:name="com.wps.enhancer.MainActivity"
            android:exported="true"
            android:label="WYU-Monet"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.wps.enhancer.DoujieActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <receiver
            android:name="com.wps.enhancer.WPSModule$CheckinReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.wps.enhancer.CHECKIN_ACTION" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.wps.enhancer.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <service
            android:name="com.wps.enhancer.WPSModule$CheckinService"
            android:exported="true" />
    </application>
</manifest>
